# --- Optimize-Media local artifacts (do not commit) ---

# Portable tool binaries (re-downloaded by setup.ps1)
tools/

# Compressed originals backup
backup/

# Processing temp files
temp/

# Run logs, reports, history (may contain private paths)
logs/

# Machine-specific config (tool paths, GPU info) - regenerated by setup.ps1
config.json

# --- Windows ---
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
*.tmp
